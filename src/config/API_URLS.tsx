import { API_BASE_URL } from "@env";

const API_URLS = {
  BASE_URL: API_BASE_URL,

  // Public
  DOCTORS: "/doctors",
  EVENTS: "/events",
  NEWS: "/news",
  SLIDES: "/slides",
  BLOG: "/blogs",

  // Patient
  APPOINTMENTS: "/appointments",
};

export default API_URLS;
